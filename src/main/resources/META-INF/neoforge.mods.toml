modLoader="javafml"
loaderVersion="*"
license="LGPL-3.0-only"
issueTrackerURL="https://github.com/RelativityMC/ScalableLux/issues"
clientSideOnly = false
showAsResourcePack = false
showAsDataPack = false

[[mods]]
modId="scalablelux"
version="${version}"
displayName="ScalableLux"
logoFile="assets/scalablelux/icon.png"
authors="Spottedleaf, ishland"
description='''Rewrites the light engine to fix lighting performance and lighting errors'''
displayURL = "https://github.com/RelativityMC/ScalableLux"

[[dependencies.scalablelux]]
modId = "minecraft"
type = "required"
versionRange = "[${minecraft_version},)"
ordering = "NONE"
side = "BOTH"

[[mixins]]
config="scalablelux.mixins.json"

# Sable physics mod compatibility: this mixin set marks Sable's per-sub-level LevelLightEngine
# instances so ScalableLux skips wiring StarLightInterface into them. Conditionally loaded only
# when the Sable mod is present.
[[mixins]]
config="scalablelux-sable.mixins.json"
mods=["sable"]
